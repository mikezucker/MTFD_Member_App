import SwiftUI

struct UniformsView: View {
    var body: some View {
        AppScreen(title: "Uniforms") {
            Text("Uniforms Module")
                .foregroundColor(.white)
        }
    }
}
