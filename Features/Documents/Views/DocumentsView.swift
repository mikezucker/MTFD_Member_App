import SwiftUI

struct DocumentsView: View {
    var body: some View {
        AppScreen(title: "Documents") {
            Text("Documents Module")
                .foregroundColor(.white)
        }
    }
}
